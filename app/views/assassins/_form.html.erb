<%= form_for(@assassin) do |f| %>
  <% if @assassin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assassin.errors.count, "error") %> prohibited this assassin from being saved:</h2>

      <ul>
      <% @assassin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
